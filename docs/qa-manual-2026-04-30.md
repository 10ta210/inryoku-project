# QA Manual Notes — 2026-04-30

## Purpose

Worker C added static regression coverage for:

- HTML shell contract across `index.html`, `p3_test.html`, and legal/support pages
- Local asset reference integrity for scripts, styles, icons, manifests, and intra-site HTML links
- Bootstrap contract for the `three` importmap, particle script ordering, and post-load guard layers

These tests catch broken file moves, missing deploy artifacts, and shell regressions early. They do not replace browser-level validation.

## Manual Checks Still Required

### 1. Desktop flow (`index.html`)

- Open the desktop entry flow and confirm `P0 -> P1 -> P2 -> P3` advances without console errors.
- Confirm the lazy-loaded `p2_code_for_claude.js` and `p3_code_for_claude.js` paths still execute in browser, not just exist on disk.
- Verify the logo speech ring attaches after entering P3 and does not duplicate after repeated transitions.

### 2. Mobile / direct P3 flow (`p3_test.html`)

- Open on a mobile viewport and confirm direct P3 boot, BGM unlock-on-first-gesture, and cart/product modal interactions.
- Verify checkout launch still works from a real browser session.
- Validate that `error-shield`, `ai-chat-client-shield`, `states`, `i18n`, and `cosmos-layer` all initialize without ordering issues.

### 3. Support / legal pages

- Check `legal.html`, `privacy.html`, `returns.html`, `size-guide.html`, `success.html`, and `offline.html` in browser for layout breakage.
- Confirm i18n toggle rendering and language swap behavior on each page.
- Confirm support-page backlinks land on the intended flow entry (`/p1_index_for_claude.html` or `/p3_test.html`).

### 4. Accessibility and resilience spot checks

- Keyboard through modals, cart, and product selection in P3.
- Validate focus visibility, dialog escape behavior, and retry behavior on `offline.html`.
- Exercise offline/service-worker behavior manually because the new tests only validate static shell references.

## Commands Used

```bash
node --test tests/html-asset-reference.test.mjs
node --test tests/html-shell-contract.test.mjs
```
