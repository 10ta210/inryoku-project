# CLAUDE.md — inryokü Project Rules (inryoku_hp 版、2026-05-23 更新)

> Single Source of Truth は **DESIGN_P3_AVATAR.md** + **DESIGN_P1.md**。
> このファイルは技術的ルールサマリ。

## 👤 役割定義
- 司さん = 非エンジニアのディレクター兼アーティスト
- Claude = プロのクリエイティブ・エンジニア
- 常に日本語でコミュニケーション
- デザイン判断は必ず立ち止まって確認を求める

## 🚫 絶対禁止ルール (不変)
- **Canvas 2D (`getContext('2d')`) 完全禁止** — Three.js + GLSL のみ
- `rm` / `del` などのファイル削除コマンド禁止
- 承認なしの大規模リファクタリング禁止
- P0/P1/P2/P3 のコード削除禁止
- 公開物にフルネーム禁止 (GREY 等のあだ名 OK)

## 📦 技術スタック制約 (2026-05-23 更新)

### Three.js バージョン
- **以前**: 0.160.0 絶対固定
- **新規**: むやみに上げない。但し **GLTFLoader 等の必要性で更新検討可**
- 上げる時は段階的、syntax check + tests で安全確認

### テクスチャ使用ルール (2026-05-23 大幅更新)
| フェーズ | テクスチャ |
|----------|-----------|
| **P0 (1984 Mac System)** | ❌ 禁止 (GLSL 手続き生成のみ) |
| **P1 (1995 Win95)** | ❌ 禁止 (GLSL 手続き生成のみ) |
| **P2 (コード世界)** | ❌ 禁止 (GLSL 手続き生成のみ) |
| **P3 (パーティクル宇宙 + EC)** | ✅ **許可** |
| **アバター** | ✅ **許可** |
| **商品 (CLO の GLB)** | ✅ **許可** |

理由: P0-P2 はレトロ世界の美学維持 (「安易な画像貼り付けにしない」)、
P3 は EC を現実的に作るため。

### 既存スタック
- Three.js 0.160.0 (現状、必要なら更新可)
- GLSL シェーダー (ShaderMaterial / 手続き生成)
- Web Audio API
- Vanilla JS (フレームワーク禁止)
- GSAP 3.12 (P3 only、レトロ世界 P0-P2 では Motion One 派生)
- フォント: 'MS Sans Serif' / VT323 / Press Start 2P / monospace

## 🎬 フェーズ構成
- **P0**: 1984 Mac System (タイトル画面 + ダイアログ + 6色球)
- **P1**: 1995 Win95 ローディング画面 (同一球体 morph、白光 → 開眼 → 十字)
- **P2**: コードワールド (量子交換 / 0と1パーティクル / 陰陽球)
- **P3**: パーティクルユニバース + EC ショップ + アバター
- **モバイル**: P0/P1/P2 スキップして P3 直行

## 🎭 P0-P3 を貫くストーリー (2026-05-23 確定)
ピクトグラム = 走る人 = i (ユーザー) は P0 から貫く一本の物語:
```
P0-P1: ローディングバー上をピクト (走る人) が走る = 最初から i
P1 climax: バー 101% → UI が球に吸い込まれる + ピクトも一緒に吸い込まれる
EXIT → ENTER: 同じ記号が視点で意味を変える
P3 (ENTER の先): 吸い込まれた i がパーティクル宇宙に現れる
```
**「誕生」ではなく「ずっといた自分に気づく / 色が灯る」**。
独立した「誕生演出」として作らない。既存 P1 吸い込み演出の延長として実装。

## 🎨 デザイン哲学
- RGB (光・精神・デジタル) を混ぜると白
- CMY (物質・アナログ) を混ぜると黒
- 白と黒の間 = グレー = 現実 = 50%
- グレーの中に虹がある (RGBCMY が眠っている)
- 視点の転換 = 50% → 101%
- アバター = i (自己) / パーティクル宇宙 = o (全体) / 他ユーザー = ü (他者)
- 「混ぜる」が正解、「重ねる」 (additive) は不正解

## 🎯 同一球体モーフ仕様 (P1)
[陰陽] → [グレー] → [RGBCMY] → [白光球] → [閉じた瞳] → [開眼] → [十字]
全て同じ Three.js Mesh の uniform 駆動 morph。別 mesh 切替禁止。

## 🌐 P3 EC (2026-05-23 進化)
**全商品をピクトグラムがモデルとして着る**:
- 商品カード中身 = 服を着たピクトグラム 3D
- モデル = ピクトグラム = ユーザーのアバター (同じ存在)
- 一発で inryokü と分かる = 発信に効く
- 3D で回せる (背中のプリントも見える)

## 🎵 音響仕様
- P0: CMY = Am / RGB = AMaj (Web Audio OscillatorNode)
- P1: 110Hz drone + 倍音段階 (cosmos-audio-harmonic.js)
- P3: 商品 hover で 80ms sub-audible chime (将来)

## 💾 安全ルール
- 重要なロジック変更前は必ずコメントで古いコード保持
- 変更は小さく・段階的に
- 変更後は「ブラウザでリロードして確認してください」と伝える
- 確認 URL: http://127.0.0.1:3000/p3_test.html (P3 単体)

## ⚡ API節約ルール
- 会話が長くなったら /compact 提案
- 単純作業は Haiku モデル推奨
- 曖昧な指示は実行前に確認

## 📚 関連ドキュメント
- `DESIGN_P1.md` — P1 同一球体モーフ設計書
- `DESIGN_P3_AVATAR.md` — P3 アバター全体構想
- `p3_ec_polish.css` — EC デザインポリッシュ (Iridescent Terminal)

## 📋 過程 (作る順番、2026-05-23)
1. **過程 A**: P0〜P3 通しで完成 (進行中)
2. **過程 B**: EC 成立 = 服が買える (Shopify variant 未設定、司さん作業)
3. **過程 C**: 商品カードにピクトグラムモデル (CLO→GLB→GLTFLoader、未着手)
4. **過程 E**: アバター調整 (染まり/着せ替え/体型、未着手)
5. **過程 F**: 育成アプリ (将来)

主戦場: A・B・C。E は P3 作り込み中。F は人が集まってから。
